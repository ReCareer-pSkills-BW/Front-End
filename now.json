{
    "version": 2,
    "name": "pskills",
    "builds": [{ "src": "pskills/package.json", "use": "@now/static-build", "config": {"distDir": "build"} }],
    "routes": [
        { "src": "/(.*)", "dest": "pskills/$1" }
    ]
}
